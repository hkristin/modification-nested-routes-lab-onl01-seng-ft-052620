<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.artist_select(@song, @path) if @song.artist %><br>
  <br />

  <%= f.submit %>
<% end %>
