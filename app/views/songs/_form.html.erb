<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.hidden_field :artist_id %>
  <%= f.select_tag(:artist_id) %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>

  <%= f.submit %>
<% end %>
